FROM node:20-alpine

WORKDIR /app

# Cai dat dependencies truoc de tan dung Docker cache
COPY package.json package-lock.json ./
RUN npm install

# Copy toan bo source code
COPY . .

EXPOSE 3000

# Chay che do development ho tro hot-reload
CMD ["npm", "run", "dev"]
